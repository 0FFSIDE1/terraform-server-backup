# Output Reference

See `outputs.tf` for authoritative output descriptions. Outputs expose the backup vault name, backup plan name and ID, backup role ARN, KMS key ARN, and optional DLM lifecycle policy ID.

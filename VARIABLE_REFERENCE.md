# Variable Reference

See `variables.tf` for authoritative types, defaults, validation, examples, and warnings. Required variables are `aws_region`, `project`, and `environment`. Optional variables tune explicit instance selection, schedules, retention, DLM AMI behavior, and tags.
